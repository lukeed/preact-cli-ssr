{
  "private": true,
  "name": "preact-cli-ssr-demo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "preact watch",
    "build": "preact build",
    "start": "node server"
  },
  "devDependencies": {
    "preact-cli": "^2.0.0"
  },
  "dependencies": {
    "compression": "^1.7.1",
    "polka": "^0.4.0",
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0",
    "preact-render-to-string": "^3.7.0",
    "sirv": "^0.1.2"
  }
}
