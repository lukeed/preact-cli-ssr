{
  "private": true,
  "name": "foobar",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "preact watch",
    "build": "preact build"
  },
  "devDependencies": {
    "preact-cli": "^2.0.0"
  },
  "dependencies": {
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0"
  }
}
